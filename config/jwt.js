// var jwt = require('jsonwebtoken');

// // export const token =(user, secret)=>{
// // var t = jwt.sign(user, secret);
// // return t
// // console.log(t)
// // }

// export const token = (user, secret) => {
    
//     const token = jwt.sign({user, secret, {
//       expiresIn: 3600 // 1 hour
//     })
//     return token
//   }

// console.log(module);